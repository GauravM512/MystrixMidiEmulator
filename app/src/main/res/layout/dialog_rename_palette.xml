<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/settings_dialog_background"
    android:minWidth="280dp"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/renamePaletteTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#F6F7FB"
        android:textSize="20sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/renamePaletteInput"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:autofillHints=""
        android:background="@drawable/settings_field_background"
        android:importantForAutofill="no"
        android:inputType="textCapWords"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:singleLine="true"
        android:textColor="#F6F7FB"
        android:textColorHint="#6F7784"
        android:textCursorDrawable="@null"
        android:textSize="15sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:gravity="end"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/renamePaletteCancelButton"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:text="@android:string/cancel"
            android:textColor="#B9C0CA"
            app:ensureMinTouchTargetSize="true"
            tools:ignore="TouchTargetSizeCheck" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/renamePaletteSaveButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:minWidth="88dp"
            android:minHeight="48dp"
            android:text="@string/setting_palette_rename_save"
            android:textColor="#FFFFFF"
            app:backgroundTint="#E43F5A"
            app:cornerRadius="8dp"
            app:ensureMinTouchTargetSize="true"
            tools:ignore="TouchTargetSizeCheck" />
    </LinearLayout>
</LinearLayout>
